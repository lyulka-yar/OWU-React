
export class calcService {

    calculating (result) {
               const value = result + number
        value > 0
            ? this.result = value
            : this.result = 0
     return value;
    }

}
